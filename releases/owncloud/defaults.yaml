# These default values can be overridden per environment in config/<env>.yaml files

chart_version: "11.0.24"

installed: false

host: "owncloud.example.com"

user:
  password: "admin"
  email: "admin@example.com"
  username: "admin"

db:
  rootPassword: ""
  username: "owncloud"
  password: "owncloud"

