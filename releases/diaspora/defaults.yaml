# These default values can be overridden per environment in config/<env>.yaml files

chart_version: "0.1.3"
installed: true

host: "pod.example.com"

db:
  username: "diaspora"
  password: "diaspora"

